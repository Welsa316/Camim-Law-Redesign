node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store
.claude/
*.log
.vercel
.netlify
docs/.parts/
# The phase 1 preview is tracked, not ignored: Vercel deploys it straight from
# the repository with the project's Root Directory set to demo/ and no build
# step, which is what keeps the deployed surface to the two reviewed pages.
# Rebuild it with site/scripts/build-demo.sh, which recreates the folder.
demo.zip
