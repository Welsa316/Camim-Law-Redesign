# Where consultation enquiries are delivered. Until these are set the endpoint
# accepts the lead, logs it, and reports success to the visitor rather than
# failing in front of them. Supply before launch.
LEAD_INBOX=
LEAD_FROM="Campos Immigration Law <no-reply@camimlaw.com>"
RESEND_API_KEY=
